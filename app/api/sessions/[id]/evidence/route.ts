import { NextResponse } from "next/server"; import { fixedEvidence } from "../../../../../lib/domain"; import { getSession,updateSession } from "../../../../../lib/store";
export async function GET(_req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await getSession(id);if(!s)return NextResponse.json({error:"Session not found."},{status:404});if(s.status==="open")return NextResponse.json({error:"Evidence is unavailable before lock."},{status:403});if(!s.evidenceRevealedAt){await updateSession(id,x=>({...x,status:"revealed",evidenceRevealedAt:new Date().toISOString()}));}return NextResponse.json(fixedEvidence);}
