import { NextResponse } from "next/server"; import { interpretationSchema } from "../../../../../lib/domain"; import { getSession,updateSession } from "../../../../../lib/store";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await getSession(id);if(!s)return NextResponse.json({error:"Session not found."},{status:404});if(s.status==="open")return NextResponse.json({error:"Lock the commitment first."},{status:409});const p=interpretationSchema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Complete the interpretation fields."},{status:400});await updateSession(id,x=>({...x,...p.data,interpretation:p.data.interpretation,status:"interpreted"}));return NextResponse.json({saved:true});}
