import { NextResponse } from "next/server"; import { commitmentSchema } from "../../../../../lib/domain"; import { lockSession } from "../../../../../lib/store";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const parsed=commitmentSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Complete every commitment field before sealing."},{status:400});const result=await lockSession(id,parsed.data);if(result.status==="missing")return NextResponse.json({error:"Session not found."},{status:404});if(result.status==="conflict")return NextResponse.json({error:"This commitment is already sealed and cannot be edited."},{status:409});return NextResponse.json({locked:true});}