import { NextResponse } from "next/server"; import { commitmentSchema } from "../../../../../lib/domain"; import { getSession,updateSession } from "../../../../../lib/store";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await getSession(id);if(!s)return NextResponse.json({error:"Session not found."},{status:404});if(s.status!=="open")return NextResponse.json({error:"This commitment is already sealed and cannot be edited."},{status:409});const parsed=commitmentSchema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Complete every commitment field before sealing."},{status:400});await updateSession(id,x=>({...x,commitment:parsed.data,status:"locked",lockedAt:new Date().toISOString(),evidenceVersion:x.scenarioVersion}));return NextResponse.json({locked:true});}
